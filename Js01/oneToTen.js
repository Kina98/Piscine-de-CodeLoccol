// Déclaration de la variable pour stocker la somme
let sum = 0;

// Boucle pour itérer à travers les nombres de 1 à 10
for (let i = 1; i <= 10; i++) {
  // Ajouter chaque nombre à la somme
  sum += i;
}

// Afficher la somme finale
console.log("sum of numbers 1 to 10 : " + sum);